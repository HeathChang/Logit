# GitHub Personal Access Token (server-only)
# - 반드시 서버 전용 이름(GITHUB_TOKEN)으로 둔다. NEXT_PUBLIC_ 접두어 금지.
# - .env.local 은 .gitignore 에 의해 커밋되지 않는다.
# - fine-grained PAT 권장 (public_repo read 만)
GITHUB_TOKEN=ghp_xxx_replace_me
